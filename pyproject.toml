[build-system]
requires = ["setuptools>=61.0", "wheel", "setuptools_scm"]
build-backend = "setuptools.build_meta"

[project]
name = "TgCrypto"
version = "1.2.6-dev0"
description = "Fast cryptography extension for Pyrogram (fork for 3.14 support)"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []
